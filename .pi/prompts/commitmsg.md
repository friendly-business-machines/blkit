---
description: Generate a commit message for staged changes
---
# Generate a commit message

Load and follow the `commitmsg` skill from
`.claude/skills/commitmsg/SKILL.md` to generate a commit message for the
currently staged changes.
